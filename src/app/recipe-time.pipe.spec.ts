import { RecipeTimePipe } from './recipe-time.pipe';

describe('RecipeTimePipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeTimePipe();
    expect(pipe).toBeTruthy();
  });
});
